# Using Javascript for Server Sent Events

Add the desired Server-Sent Event URL into sse.html, then simply drop sse.html into a browser windows to run.

Tested in Chrome, Firefox and Safari on Mac
Tested in Edge, Chrome and Firefox on Windows 10